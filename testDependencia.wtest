import dependencia.*
import rodados.*

describe "prueba dependencias" {
    const deporte = new Dependencia(empleados=45)
    const Cultura = new Dependencia(empleados=31)

  test "corsa color rojo" {
    var cachito = new Corsa(color = rojo)
    assert.equals(rojo, cachito.color())
  }
}

